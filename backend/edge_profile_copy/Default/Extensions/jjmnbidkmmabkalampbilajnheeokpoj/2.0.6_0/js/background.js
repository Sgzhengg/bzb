// Manifest V3 Service Worker
// Note: Service workers don't have access to DOM, so we can't use jQuery here
// background.min.js only uses Chrome APIs, so it doesn't need jQuery
importScripts('background.min.js');

